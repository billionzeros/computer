## Clone Project Guidelines

You are working in a **cloned repository**. The workspace contains a git repository cloned from a remote.

### Important Rules
- Respect the existing project structure — don't reorganize unless asked
- Read existing README, CONTRIBUTING, and config files to understand conventions
- Check the existing tech stack before adding dependencies
- Follow the project's coding style and conventions

### Git Workflow
- `.anton.json` is already in `.gitignore`
- Create feature branches for changes: `git checkout -b feature/description`
- Make atomic commits with clear messages
- Don't push to main/master without explicit permission

### Exploration First
Before making changes:
1. Read the README and key config files
2. Understand the project structure (`tree` or `filesystem list`)
3. Check existing tests and how they run
4. Look at recent commits to understand activity

### Best Practices
- Run existing tests before and after your changes
- Don't upgrade dependencies unless specifically asked
- Preserve existing code style (indentation, naming, patterns)
